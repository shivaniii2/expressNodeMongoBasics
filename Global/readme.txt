just like we have window as a global object in our web browsers , Similarily we have global object in node js

we do not have document object in global namespace of node js so we can not execute like document.getElementById etc and we wont even need to do that